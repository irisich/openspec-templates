<!-- Это дельта-файл спецификации (openspec/changes/<id>/specs/<capability>/spec.md) -
     контракт "ЧТО должна делать система", без деталей реализации (детали - в design.md).
     Заголовки ниже в CAPS/по-английски ("Purpose", "ADDED/MODIFIED/REMOVED/RENAMED
     Requirements", "Requirement:", "Scenario:") жёстко зашиты в парсере OpenSpec
     (регистр не важен, но слова должны быть английскими) - переименовывать нельзя.
     Название требования/сценария после двоеточия и весь остальной текст - свободные,
     можно писать по-русски. -->

## Purpose
<!-- Только для новой возможности: 1-2 предложения (50+ символов) о её назначении.
     Для дельты к существующей возможности - раздел удалить, Purpose уже есть в основной спеке. -->

## ADDED Requirements

### Requirement: <!-- название требования -->
<!-- Текст требования. Должен содержать заглавное SHALL или MUST (например: "Система SHALL ...") -
     без этого валидатор не считает требование нормативным. -->

#### Scenario: <!-- название сценария -->
- **WHEN** <!-- условие -->
- **THEN** <!-- ожидаемый результат -->

<!-- ПРИМЕР: не входит в текущую постановку, показывает только формат заполнения -->
**Пример (новая возможность `data-export`):**

```
## Purpose

Позволяет пользователям выгружать свои данные из системы в переносимом формате.

## ADDED Requirements

### Requirement: Пользователь может выгрузить данные

Система SHALL позволять пользователю выгружать свои данные в формате CSV.

#### Scenario: Успешная выгрузка
- **WHEN** пользователь нажимает кнопку "Экспорт"
- **THEN** система формирует и скачивает CSV-файл со всеми данными пользователя
```
<!-- /ПРИМЕР -->
